# 抬杠模拟器 · 部署工作流（固化版）

> 目标：把「更新代码/美术 → 上线到轻量云」从 40 分钟压到 1 条命令。
> 服务器：Lighthouse `lhins-jchqvx7w`（`211.159.177.250:8787`，北京）
> 实例侧脚本：`/opt/bianyi/deploy.sh`（已安装、已通过 `bash -n` 语法检查）

## 一、正常更新流程（推荐，最快）

1. **你**：把最新代码+美术 push 到 GitHub 仓库 `AzoriusP/AIDebateGame` 的 `main` 分支。
2. **我**（或你直接在控制台）在实例上跑一条命令：
   ```bash
   bash /opt/bianyi/deploy.sh
   ```
3. 脚本自动完成：拉 GitHub 包 → 解包 → 保留 HY4 密钥配置 → 重启 → 自检。
4. 验证：浏览器开 `http://211.159.177.250:8787/` 返回 200 即成功。

**为什么能从 GitHub 拉**：仓库是 public，实例直连 `github.com` 的 archive 端点（实测 200），读 public 仓库不需要任何凭证。所以你管 push、实例管 pull。

## 二、deploy.sh 做了什么

| 步骤 | 动作 |
|---|---|
| 1/4 | `curl` 拉 `https://github.com/AzoriusP/AIDebateGame/archive/refs/heads/main.tar.gz` |
| 2/4 | 解包到 `/tmp/gd`，自动识别 `demo/` 目录（仓库根=代码 或 仓库根/demo 两种情况都兼容） |
| 3/4 | 备份实例旧 `config.json` → 换新 demo → 把旧配置里的 `llm_mode/openai_base/openai_api_key/openai_model` 合并回新模板（保住 HY4 密钥） |
| 4/4 | 杀旧进程 → `setsid python3 server.py` 后台重启 → 自检 health |

## 三、⚠️ 三条铁律

1. **永远不要把 HY4 密钥 commit 到 GitHub**。本地 `demo/config.json` 里 `openai_api_key` 必须保持 `""`（空）；真实密钥只存在于实例 `/opt/bianyi/demo/config.json`。一旦带密钥 push，等于公开泄露。
2. **改 config 字段时注意**：`deploy.sh` 只合并 6 个字段（llm_mode/openai_base/openai_api_key/openai_model/keep_alive/llm_opening）。若以后 server.py 新增了必读的 config 字段，要在 `deploy.sh` 第 25 行的 python 合并列表里同步加上，否则新字段会被旧模板吞掉。
3. **大美术资源（Live2D 大 PNG）会拖慢拉取**：4Mbps 带宽，29MB 约 60 秒，属正常。纯代码更新则几秒完成。

## 四、备选路径（GitHub 没更新时）

如果某次只改了本地、没 push GitHub，走「本机直推」：
1. 实例 8787 起临时接收器 `setsid python3 recv.py`（do_POST 存 `/tmp/up.tgz`）。
2. 本机 `curl -X POST --data-binary @_deploy.tgz http://211.159.177.250:8787/`（注意是 POST 不是 PUT，`-T` 会 501）。
3. 再跑解包+合并+重启（同 deploy.sh 的 2-4 步）。

## 五、限流坑（Lighthouse MCP execute_command）

- `AccessDeny` 是**间歇性**的，不是永久锁死。
- 解法：**拆小命令（≤~200 字）+ 间隔 ≥7-10s**，别拼大命令；别在实例侧 `curl 127.0.0.1` 自测（改从公网 IP 验证）。
- 装好了 `deploy.sh` 后，每次更新只需 1 条 `bash deploy.sh`（短命令），基本不再触发限流。
