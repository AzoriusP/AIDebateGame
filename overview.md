# 《辩弈》核心闭环 GDD — 交付概览

## 本次做了什么

大王提出了一款 **AI Native 辩论游戏** 的完整概念（认同度 + token 预算 + 六维图 + 天梯 + 题库/NPC 库 + Agent harness 判定）。主理人（游承峰）完成阶段诊断（Phase 0→1），派设计策略师文策渊产出核心闭环 GDD。

## 关键结论

1. **赛道定位**：2026 腾讯游戏创作大赛 · AI 游戏赛道 → **AI原生游戏** 子赛道（去掉 AI，"辩论对手 + 说服判定"不成立，正符合评审要求）。
2. **技术栈判断**：Web 全栈 + 重演出。裸聊天框拿不了奖，问题不在引擎，在"演出层"缺失。
3. **三大设计风险已识别并给出解法**：判定玄学（结构化 harness）、六维静态化=抄作业（动态漂移）、token 只是计数器（废话/错误方向惩罚）。

## 核心设计（文策渊 v0.1）

- **Fun hypothesis**：在 token 倒计时压迫下，读懂 NPC 会漂移的六维人格，精准命中动态薄弱点。
- **4 Design Pillars**：先读懂再出拳 / 对手是活的 / 每一句话都是资源 / AI 是裁判不是神谕。
- **六维**：逻辑力、证据力、情感力、利益力、认同力、权威力（亚里士多德三诉诸 + 行为经济学拆解）。
- **认同度判定**：论点→维度→强度(L0~L3)→查表加分（薄弱点 L3 命中 +40）。
- **Token 经济**：固定底扣 20 + 超 60 字按字数扣；难度 L1→L5 配额 420→280。
- **漂移机制**：薄弱点警觉上浮、冷落维弃守下沉、强项加固。

## 美术方向（新增，林绘澄 AD-001）

大王给的视觉锚点：逆转裁判式对立立绘 + 玩家自定义 + NPC 库 + "我有异议!"演出 + token 见底玩家神情紧张。已产出 `design/art-bible.md`：

- **视觉身份**：「言刃·对峙法庭」，三色铁三角（深邃靛蓝底 / 异议红 / 认同金）。
- **换装**：分层不捏脸（3 预设 + 4 部位），solo 可量产。
- **表情状态机**：玩家侧随 token 4 档（从容→紧张→焦虑→绝望）；NPC 侧随认同度（得意→警觉→动摇→被说服）。
- **双向异议演出**：命中薄弱点→玩家金"异议"+NPC 动摇；命中强项→NPC 红"异议"反击。
- **IP 边界**：学语法不碰内容；字体用思源开源规避 Capcom。

## 关键设计修正（大王 2026-09-04 提出）

1. **认同度不对称**：认同度是 NPC 专属属性，玩家没有认同值，LLM 永不判定"玩家被说服"。玩家失败 = 只用光 token（+ 愿景：主动点"认同"投降）。
2. **NPC 异议 = 强度曲线**：NPC 不一开始就强，随认同度下降（中后期）提升强度、主动打"异议"甩难辩论点，由 harness 判断何时加压。

## Harness 架构（程基岩 ENG-001）

`docs/architecture/harness-architecture.md`。5 条 ADR：
- **ADR-001 裁判/NPC 分离**（两条 LLM，防角色混淆+注入）
- **ADR-002 状态层确定性持有**（LLM 只读，状态迁移只走纯函数）
- **ADR-003 调度用规则、内容用 LLM**（异议触发/强度是确定性纯函数）
- **ADR-004 每回合 2 次调用**（1 判定 + 1 NPC 回复）
- **ADR-005 判定非流式 + NPC 流式**（VFX 结算后即时播，演出掩盖延迟）

**语义张力已解决**：认同度方向定案为「自信度」100→0（0=被说服=胜），方向反转后开局从容、越打越紧张、<50% 背水一战，程基岩的 warm-up 守卫自动作废。

## 待办 / 下一步

1. 数值校准（`[微调]` 项）——token 已按反馈调过一轮，待大王继续试玩确认加分档、漂移幅度。
2. 后续：NPC 扩到 15（每层 3 个）、ASR 语音输入、真·表情分层立绘（4 表情/角色）、云部署。

## 已知问题 & 待拍板的决策点

1. ~~**漂移机制半成品**~~ ✅ **已落地**：`_direction` 改为基于漂移后实际值（软肋≤4/强项≥6.5），软肋被打会上浮失效、冷落维度下沉成新软肋——"反抄作业"真正生效，玩家需持续读图换维度。已调平衡（软肋可打 2-3 次才失效）。
2. **离题检测偏弱**：qwen3.5（9.7B 本地模型）语义天花板，看到"数据/权威"就归维度，偶有漏判。换更大模型/云端 API 可显著改善。
3. ~~**选边违和**~~ ✅ **已定案：立场按 NPC 人设分配**（NPC 站人设天然支持/反对的一边，与人设无关才随机，玩家自动站对面），消除硬翻立场导致的人设违和。

## 第一版 DEMO（已完成，可玩 · 5 层天梯）

- 位置：`demo/`，启动 `demo/start.bat` 或 `python server.py` → http://localhost:8787
- 零依赖单文件后端（Python 标准库）+ 原生前端，LLM 三模式（本地 Ollama / 云端 API / Mock）
- 已跑通：立场分配→判定→自信度→token→双向异议演出→胜负 全链路；默认接用户本地 qwen3.5 模型
- **5 层天梯**：王阿姨→林经理→陈博主→赵律师→陆教授（token 420→280，软肋 3→1），题库 50 命题分层
- **立场按人设分配**：NPC 站 TA 人设的一边（neutral 才随机），玩家自动站对面；开辩前界面亮明双方立场
- **演出**：NPC 本地中文语音（按性别分音色）+ 打字机 + 异议闪屏 + 倒计时（8 分钟）
- **复盘**：结束后 AI 生成一段对话复盘（哪句说错、应该怎么说）
- **教练提示**：卡住时点「💡 提示」，AI 给一句可照抄的引导话术，拉回跑偏的话题
- **换一题**：开辩前可原地重摇命题
- **动态漂移（反抄作业）**：软肋被打会上浮失效、冷落维度下沉成新软肋，玩家需持续读图换维度，不能背住一个软肋打到死
- 关键修复：Ollama thinking 模式导致慢 → 改原生 `/api/chat` + `think:false`，回合 ~1.5s

## 数值推演结论（demo/playtest.py，纯数值模拟，可重跑）

当前 token 配额已按试玩反馈上调（TOKEN_FREE=60），模拟「全 L2 命中软肋」的余量：

| NPC | token | 全 L2 命中余 token | 难度感 |
|---|---|---|---|
| L1 王阿姨 | 420 | +320 | 宽松（新手关） |
| L2 林经理 | 380 | +280 | 宽松 |
| L3 陈博主 | 340 | +240 | 适中 |
| L4 赵律师 | 300 | +200 | 适中 |
| L5 陆教授 | 280 | +180 | 有容错（软肋仅 1 个，难点在精准） |

对比上一版：L5 从「全 L2 命中余 -10（极限）」缓解到「+180」，L2/L0 交替打法也能通关（余 60）。难度梯度 420→280 保持，L5 的挑战感主要来自「软肋只有 1 个」而非 token 卡死。

## v2 里程碑（2026-09-04 全自动执行：P0→P2-1 完成，P2-2 已出设计稿待确认）

设计依据：`design/gdd/systems-v2.md`（复盘克制 / 双模式 / NPC 记忆成长 / 动态人格流水线，已定稿）。

| 阶段 | 内容 | 状态 |
|---|---|---|
| P0-1 | **复盘克制版**：复盘不再输入六维数值，只评表达质量（空泛/啰嗦/被带偏），不喂数值攻略 | ✅ 已实现 |
| P0-2 | **NPC 记忆与成长底座**：`data/npc_state.json` 持久化；局终生成 ≤45 字第一人称记忆（NPC 视角，不提数值）；被打维度六维微调 ±2 钳制、冷落回弹；`/api/npc_reset` 一键重置；NPC prompt 注入记忆（翻旧账） | ✅ 已实现 |
| P1-1 | **天梯系列赛**：锁定同对手三局两胜、每局换题；前端模式入口 + 系列赛横幅（第几局/大比分）+ 晋级/淘汰结算；**逐级解锁**（`data/progress.json` 记录最高通关层，战胜本层才解锁下一层，未解锁层锁定灰显） | ✅ 已实现 |
| P1-2 | **自由切磋 + 记忆开关**：主界面双模式；自由模式任意对手、记忆开关（默认关=纯净） | ✅ 已实现 |
| P2-1 | **表格化 + AI 生成流水线**：`tools/sync_csv.py`（JSON↔CSV 双向，往返一致性通过）；`tools/gen_npcs.py`（调本地 LLM 批量生成 NPC 候选 → `npc_candidates.json`，含一致性清洗；实测 L1 生成质量达标） | ✅ 已实现 |
| P2-2 | **美术演出设计稿**：`design/art-bible-v2.md`（分层表情立绘规格、晋级/淘汰仪式演出、NPC 记忆可视化、玩家形象、音频方向、5 个待拍板决策） | 📝 已出稿待大王确认 |

**剩余已知问题**：① 离题检测偏弱（9.7B 本地模型天花板，云端 API 延后测）；②（已解决）NPC 立场曾可被玩家自由选择而硬翻人设，现改为按人设分配。

## 美术提升汇编（2026-09-08 · Phase 6 打磨 · ART-002 + ENG-ART-001）

诊断：美术处在「原型占位」级。三份文档已产出并完成跨成员一致性核验：

| 产物 | 路径 | 要点 |
|---|---|---|
| 美术提升路线图 | `design/art-polish-roadmap.md` | 12 项审计（每张立绘带 AI 水印、无透明通道、玩家朝向反）；P0 全清 6.5–8 人日；**P3 骨骼动画不做**；AI 生图管线三段式提示词模板 |
| 前端演出层改造方案 | `docs/frontend-polish-plan.md` | 首屏 9MB 元凶 = 1.5MB 原图当 56px 缩略图；表情改造 = 纯前端 + 后端 5 行，**图没出也能零白屏降级**；9 步实施约 9h，S0–S7 可与生图并行 |
| 资产命名接口（裁决） | 两文档 §4.4 / §2.1 | 统一为 `portrait/{npc_id}/{state}.webp` 分目录 + npc_id 主键，扁平命名作废 |

**实测核实**：6 张 PNG 均 1024×1024 无 alpha 合计 8.5MB；右下角「AI生成 WORKBUDDY>」水印属实（重生成时去水印 + 页脚合规声明）。

## P0 执行进度（2026-09-08 · 大王拍板「全自动做 P0」后落地）

工程线 **已完成并端到端验证通过**（server.py/index.html 此前已被先前会话部分改造，本轮补齐 app.js 旧逻辑 + style.css 缺失规则 + 资产转换）：

| 项 | 内容 | 状态 |
|---|---|---|
| S0 资产转换 | `tools/optim_portraits.py` 把 6 张 PNG(8.5MB)→ `portrait/{id}/{calm,thumb}.webp`，总量 389KB，单张 ≤96KB | ✅ |
| S1 后端 | avatar 已 npc_id 化 + avatar_thumb/base/legacy + Cache-Control no-cache | ✅（先前已改，本轮验证） |
| S2 表情状态机 | `setPortrait` 双层 crossfade + 中文→英文映射 + 三级降级链 + 预取 | ✅ |
| S3/S9 降级开关 | `bianyi_fx` 持久化 + 主界面「⚙ 演出设置」+ prefers-reduced-motion | ✅ |
| S4 演出修复 | 异议闪屏重写（常驻 ob-glow 双层 + D2 重放修复）+ vignette 暗角 + confidence scaleX + 缩略图 D1 修复 | ✅ |
| S5/S6 结算演出 | 晋级金印落下 + 胜利粒子 + 淘汰灰暗化 | ✅ |
| S7 翻旧账 | `memory_recall` → 开局金色 toast「TA 还记得上次…」 | ✅ |
| S9 性能 | 打字机 scrollTop 节流（每 4 字） | ✅ |

**端到端验证**：静态资产 200/webp、Cache-Control 生效、/api/tiers 新字段、/api/new_session 返回 npc_id+memory_recall、/api/message 返回 emotion.npc=得意/player=从容 全通过。

**待办（美术线）**：18 张表情图（6 角色×3 新表情，calm 已由现有图转换）待生图——需要能目检图的环节，ImageGen 生图 + 大王验收一致性。

**已知遗留**：① mock 模式下 npc_state.json 的 L1_A 有 3 条「数数数…」脏记忆（影响翻旧账文案，可用主界面「重置 NPC 记忆」清理）；② 当前 config.json 为 `llm_mode: mock`，跑真 LLM 需改回 `ollama`。

## 分层角色系统立项（2026-09-08 · 2D 动画 + 捏人）

大王新需求：角色 2D 动作动画 + 玩家捏人（换性别/脸/头发/衣服），推翻之前「P3 骨骼动画不做、换装降级」。技术选型已定，见 `design/character-system-design.md`：

| 决策 | 结论 |
|---|---|
| 动画技术 | **分层 sprite + CSS 骨架动画**（零成本、最适合 AI 分层生成、纯 Web 零依赖），否决 Spine/DragonBones |
| 捏人 | 自由拼装 性别(2)×脸(3)×头发(3)×衣服(3)，Picrew 式 |
| 分层架构 | body/head/outfit/hair 四层（head 含表情变体），统一 576×864 画布锚点 |

**本轮已落地（C0 前端）**：CSS 骨架动画（呼吸/点头/后仰/垂头）、捏人数据层（`bianyi_avatar` 持久化）、player-avatar 分层结构 + 主界面角色卡 + `applyAvatar` 拼装引擎 + 表情状态机适配（部件未就绪自动降级整图）。

**关键发现**：ImageGen 的 `background: transparent` 参数**不生效**（固定灰底），改「生图 + rembg 抠图」后处理（`tools/segment_parts.py`），rembg 正在装。

**下一步**：rembg 装好 → 抠图 → 生成部件资产（2 身体×3 脸×3 头发×3 衣服 + 表情）→ 大王目检拼装对齐。

